# Zig build artifacts
zig-cache/
zig-out/
.zig-cache/

# Build outputs
*.o
main
zigzag

# Model files (too large for git)
models/*.onnx
models/*.safetensors
models/*.bin

# Tokenizer testing scripts (not part of main repo)
tokenizer_example/

# OS files
.DS_Store
